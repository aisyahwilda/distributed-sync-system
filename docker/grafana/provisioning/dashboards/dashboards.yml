apiVersion: 1

providers:
  - name: "Distributed Sync"
    orgId: 1
    folder: "Distributed Sync"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
